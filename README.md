# kentscript
A systems programming language combining Python-like syntax with C-like low-level features. Features interpreter mode with full stdlib, native compilation via C transpiler, interactive REPL with autocomplete, built-in GUI toolkit, and low-level features (pointers, syscalls, inline assembly). Created by pyLord (Uganda)
